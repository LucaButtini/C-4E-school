#include <stdio.h>
#include <string.h>
int main(int argc, char *argv[])
{
    //il confronto fra due stringhe si fa con strcmp. Se il valore che ritorna è 0 sono uguali

    /*strcmp str1+ lunga di str2
    starcmp str1 + corta di str2
     if (strcmp (str1, str2)==0{
        printf("Le due stringhe sono uguali");
        
    }else{
        printf("Le due stringhe sono diverse");
    }         */



    //strcat concatena le stringhe. Le unisce è le concatena dalla sorgente
    /* strcat (str1, str2);
        printf("Lea stringa concatenata è: %s", str1);
        strcpy(str1,str2);
        printf("La stringa copiata è %s", str1);
    */
    return 0;
}